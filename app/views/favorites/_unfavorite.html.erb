<%= link_to favorite_path(current_user.favorites.find_by(training_id: training.id)),
    id: "unfavorite-button-for-training-#{training.id}",
    data: { turbo_method: :delete } do %>

    ★
<% end %>